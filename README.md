Add this line to `~/.lldbinit`
```
command script import ~/.lldb/option.py
```
suppose the repo is located in `~/.lldb`.

Or just run the import in lldb shell:
```
(lldb) command script import option.py
```
